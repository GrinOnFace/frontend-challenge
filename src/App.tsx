import { FC, useEffect } from "react";
import { observer } from "mobx-react-lite";

export const App = (() => {

    return (
        <div className='app'>
			<a href="/likes">Любимые котята</a>
        </div>
    );
});

