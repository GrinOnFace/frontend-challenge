import { FC } from 'react';
import { Link } from 'react-router-dom';

const Likes: FC = () => {
    return (
        <div>
            <Link to={"/likes"}>Link</Link>
			
        </div>
    );
};

export default Likes;
